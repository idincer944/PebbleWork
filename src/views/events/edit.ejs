<!-- <% if (user && user.id === article.author.id) { %>
    <h1 class="mb-4">Edit Article</h1>
    <% } else { %>
    <h1 class="mb-4">View Markdown</h1>
    <% } %>
    
    <form action="/articles/<%= article.id %>?_method=PUT" method="POST">
      <%- include('_form_fields') %>
    </form> -->